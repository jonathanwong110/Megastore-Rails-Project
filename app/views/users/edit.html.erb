<div class="heading-for-one">

<h2> Edit your Account </h2>

  <br></br>

  <b>Please note that a password must be entered to submit changes. Please submit the same password if you do not want to change the password. </b>
    
  <br></br>
  <br></br>

  <%= form_for @user do |f| %>
      <%= f.label :email, "Email:" %>
      <%= f.text_field :email %> <br></br>
      <%= f.label :password, "Password:" %> 
      <%= f.password_field :password %> <br></br>
      <%= f.submit %>
  <% end %>

  <br></br>

  <h2> Delete your Account </h2>
  <br></br>
  <b> Are you sure? </b> <br></br>
  <%= link_to "Delete Account", user_path(current_user.id), method: :delete %> <br></br>

  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>
  
      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

</div>